<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <title>To Do List</title>
  <link rel='stylesheet' href='https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css'>
  <link rel='stylesheet' href='~/css/styles.css' type="text/css">
</head>
<body>
  <div class="jumbotron">
      <h1>Cut n' Paste Salon</h1>
    </div>
      <div class="container">
      <h2>Select Specialty</h2>
      <ul>
        @if (Model.Count == 0)
        {
          <li>No specialties have been added yet!</li>
        }

        @if(Model.Count != 0)
        {
          @foreach (var specialty in Model)
          {

            <a href="/specialties/@specialty.GetId()">@specialty.GetType()</a><br>
          }
        }
      </ul>

      <!-- <form action="/stylists/delete" method="post">
       <button class="btn btn-danger" type="submit">Delete all stylists</button>
     </form> -->
     <a href="/specialties/new">Add Specialty</a>
      <a href="/">Back to homepage</a>
  </div>
</body>
</html>
